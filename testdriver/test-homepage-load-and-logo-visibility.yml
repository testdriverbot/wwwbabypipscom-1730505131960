version: 4.0.79
steps:
  - prompt: Assert that the homepage loads successfully.
    commands:
      - command: hover-text
        text: Start Learning
        description: button on the homepage
        action: hover
      - command: assert
        expect: the homepage loads successfully
  - prompt: >-
      Assert that the website logo is visible.  # Use the content of the file
      from the environment
    commands:
      - command: hover-text
        text: babypips
        description: website logo text
        action: hover
      - command: assert
        expect: the website logo is visible
